---
// src/pages/blog/index.astro
import Blog from "../layouts/components/Blog.astro";

// Strapi'den veri çekme
const response = await fetch(`${import.meta.env.STRAPI_URL}/api/posts?populate=*`);
const { data: posts } = await response.json();
---

<Blog posts={posts} />